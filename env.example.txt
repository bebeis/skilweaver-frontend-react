# API 설정
# Vercel 배포 시: Vercel 대시보드에서 환경 변수로 설정
# 로컬 개발 시: .env.local 파일 생성 후 아래 값 설정

# EC2 서버의 API 베이스 URL
# 예: https://api.skillweaver.com 또는 http://your-ec2-ip:8080
VITE_API_BASE_URL=http://localhost:8080/api/v1

# 개발 환경에서는 /api/v1로 설정하고 vercel.json의 rewrites를 통해 프록시
# 프로덕션 환경에서는 실제 EC2 서버 URL을 설정

